#!/bin/sh

# description:
#   alias for run spec set.
#   /controllers/models/env/deploy
# example:
#   $ script/spec_set

set -e

if [ ! $DB ] ; then export DB=sqlite; fi

# migrate
RAILS_ENV=test bundle exec rake db:migrate

# run spec
bundle exec rspec spec
