<% if user_signed_in? %>
  <li>
  Logged in as <%= current_user.name %>
  </li>
  <li>
  <%= link_to('signout', signout_path, { id: :signout }) %>
  </li>
<% else %>
  <li>
  <%= link_to('signin', signin_path,  { id: :signin }) %>
  </li>
<% end %>
